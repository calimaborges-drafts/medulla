{
  "name": "medulla-client",
  "private": true,
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "tsc -p tsconfig.json && cross-env NODE_ENV=development node dist/index.js",
    "test": "jest",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/jest": "^23.3.3",
    "cross-env": "^5.2.0",
    "jest": "^23.6.0",
    "ts-jest": "^23.10.4",
    "typescript": "^3.1.1"
  }
}
